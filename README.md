# httpc
### A "minimal" library for HTTP servers for the "best" programming language
#### (p.s: don't actually use this)


# todo
- better parser for requests
- header reading / cookies / body parsing for request
- custom path handlers w/ function pointers
- thread pool (please stop procrastinating on this)

